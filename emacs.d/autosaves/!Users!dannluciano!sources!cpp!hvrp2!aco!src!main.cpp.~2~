#include <iostream>
#include "colony.hpp"
#include "instance.hpp"


using namespace std;

int main(int argc, char *argv[])
{
		if (argc >= 2 )
		{
				cout << "Reading Isntance File" << endl;
				Instance problem = Instance::ReadConfigFile(argv[1]);
				cout << problem << endl;

				Colony colony = Colony(problem);
				
		}
  
  return 0;
}
